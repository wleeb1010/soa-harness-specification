# Conformance Test Program — SOA-Harness §9.2

This is a pinned, minimal `program.md` for conformance testing of `SV-SIGN-02` (detached JWS envelope shape) and `SV-SIGN-05` (two-step signer resolution via x5t#S256 thumbprint header). Do not edit without regenerating the companion `.jws` files via `generate.mjs`.

## Self-improvement directive

The agent MAY propose iterative refinements to its own prompts and tool descriptions within the editable surfaces declared on the Agent Card, subject to §9 constraints including novelty quota, benchmark fidelity, and §9.7 seccomp profile enforcement.

## Scope

Conformance fixture only. This program.md is intentionally minimal; real deployment programs will carry operator-specific directives, benchmarks, and editable-surface declarations.
